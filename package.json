{
  "name": "threat-outlook-api",
  "version": "1.0.0",
  "description": "Backend API for Daily Threat Outlook generation",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
```

**File 3: `.env.example`** (for reference)
```
CLAUDE_API_KEY=your_claude_api_key_here
PORT=3000
